import { LoginPage, generateMetadata } from '../../components/auth/server/LoginPage'

// Re-export metadata function
export { generateMetadata }

// Re-export the LoginPage as the default export
export default LoginPage
