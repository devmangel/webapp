import { SignupPage, generateMetadata } from '../../components/auth/server/SignupPage'

// Re-export metadata function
export { generateMetadata }

// Re-export the SignupPage as the default export
export default SignupPage
